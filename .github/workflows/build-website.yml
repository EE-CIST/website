name : build-site

on:
  push:
    branches: [ main ]

jobs:
  container-quarto:
     runs-on: ubuntu-latest
     container:
        image: sreycoyrehourcq/cist-ee:main
     steps:
       - name: check quarto
         run: (quarto check)
         working-directory: project

       - name: check renvlock
         run: (cat renv.lock)
         working-directory: project
